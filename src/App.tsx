import { Header, Listing } from 'components';
import React from 'react';
import styled from 'styled-components';

function App() {
	return (
		<>
			<Header />
			<Listing />
		</>
	);
}

export default App;
